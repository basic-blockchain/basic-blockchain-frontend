# v0.8.2 — Maintenance & GitFlow Integration

Pairs with simulator **v0.14.2**. Maintenance release completing Phase I.5 admin
enrichment cycle. Consolidates all integration branch updates and resolves branch
promotion constraints.

## Changes

### Integration & Promotion

- Merged `integrate/feature-exchange-feeds-frontend` into `develop` with full
  CI validation (lint, typecheck, test, build, SAST).
- Completed GitFlow chain: `qa` → `develop` → `production` → `main` with proper
  branch protection alignment.
- Updated branch protection contexts to match GitHub Actions workflow names
  (`CI / Lint · Typecheck · Test · Build`, `CI / Dependency Audit`,
  `SAST / CodeQL`, `SAST / Semgrep SAST`, `Merge Policy Guard / enforce-merge-policy`).

### Bug Fixes

- Fixed auth token timing edge case from v0.8.0 (ensure JWT available before
  calling `/auth/me`).
- Resolved admin UI display issues with frozen wallet states.

## Notes

- Release completes Phase I.5. Next major work planned for Phase J (WebSocket
  performance and real-time state sync optimization).
- All promotion PRs validated and closed cleanly. Branch protection rules
  restored to production levels.
